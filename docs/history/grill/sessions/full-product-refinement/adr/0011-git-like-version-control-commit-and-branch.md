# 快照升级为类 git 版本管理：commit + branch

快照从简单备份点升级为类 git 版本管理：显式创建快照 ≈ commit（可填备注）；引入 branch，用于平行剧情试写与大改安全网；支持 checkout 切换分支继续写。存储采用内容寻址去重（未变文件共享存储），仍存作品目录元数据区（如 `.teller/snapshots/`）随作品迁移。自动保存仍不产生版本（ADR 0007 边界不变）。此条推翻 ADR 0006/0007/0014/0055 中的「版本分支 / Git 提交」回避立场。
