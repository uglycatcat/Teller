# 垃圾箱仅从「作品」菜单进入；保留期可配置

删除的章节/设定进入该作品目录内的垃圾箱。完整产品形态下：

- **入口：** 仅菜单「作品 → 垃圾箱」打开管理页；左侧资源侧栏不设垃圾箱入口，避免与章节/设定抢注意力  
- **操作：** 列表支持恢复、立即永久删除、清空垃圾箱  
- **保留期：** 默认 30 天自动清除；在应用设置中可配置为 7 / 30 / 90 天或永不自动清除  

垃圾箱仍按作品隔离，不使用应用全局统一垃圾箱，也不把系统回收站当作唯一手段。
