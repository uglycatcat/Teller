# Teller

面向长篇创作者的 AI 原生桌面写作工作台。

> **文档状态（2026-08-25）**：本文档保留历史决策；最新完整产品定义以 `docs/prd/` 为准。full-product-refinement 会话的增量（快照升级为类 git 版本管理、章节状态、正文图片资源目录 `assets/`、字数双口径、仅 Windows 手动更新等）已并入 `docs/prd/`，本文档对应条目已同步修订。

## Language

**作品 (Work)**：
作者正在创作的一部长篇内容整体，是章节、设定与版本所归属的根对象；其下的一等结构为章节与设定，不以「分卷树」或独立「大纲对象」作为产品模型。可通过「作品 → 重命名」修改显示标题，并可选同步重命名作品文件夹。显示标题存于作品目录元数据（如 `.teller/work.json`），可与文件夹名不同；缺失时回退为文件夹名。
_Avoid_：项目, 书, 文档, 文件, 强制分卷树, 独立大纲/细纲实体, 改标题必须同时成功改文件夹否则禁止, 标题仅存在应用全局库不进作品目录

**章节 (Chapter)**：
一部作品中按阅读/写作顺序排列、可单独打开编辑的正文单元；需要分卷或大纲时，用章节命名约定或设定文稿表达，而不是挂到单独的卷/大纲节点上。侧栏新建时作者只填标题，由应用写入带三位数字前缀（如 `001`）的文件名。侧栏与标签所显示的章节标题以文件名中序号后的标题段为准（如 `001-开篇.md` →「开篇」）；不以文首标题或 frontmatter 为权威。同一作品内章节标题须唯一；新建或重命名冲突时**自动加后缀**（如「番外 2」）。新建章节时文稿预填较完整模板（含与标题对应的一级标题、分隔与简短写作提示），作者可改可删。每章一个状态标记，存文稿首部 YAML frontmatter（`status`），三态为草稿 / 进行中 / 定稿，随文件走；frontmatter 仅承载状态等字段，不作为标题权威。不提供与设定之间的一键互转。
_Avoid_：篇, 话, 文档, 文件, episode, 必须从属于分卷节点, 要求作者手写序号文件名, 以文首 H1 或 YAML frontmatter 为标题权威, 章节与设定一键互转, 允许同作品内章节标题重复, 重名时仅拒绝而无自动消解, 新建章节正文强制空白无模板

**设定 (Setting)**：
归属某部作品、供写作时查阅的非正文资料条目；每条设定是一篇可独立打开编辑的文档（人物、世界观、力量体系等均以此承载）。侧栏操作与章节同一套：按标题新建、可拖拽排序、删除进入作品垃圾箱；排序同样以文件名数字前缀落盘（如 `001-主角.md`）。侧栏显示标题同样以文件名中序号后的标题段为准。同一作品内设定标题须唯一；新建或重命名冲突时**自动加后缀**（如「主角 2」）。新建设定时文稿预填设定向模板（标题 + 如别名/外形/性格等提示小节，偏资料卡而非章节写作提示），作者可改可删。不提供与章节之间的一键互转。
_Avoid_：笔记, Wiki, 资料, 世界书, lore, 结构化卡片, 与章节两套不同操作模型, 设定顺序仅存 UI 不落盘, 以文首 H1 或 YAML frontmatter 为标题权威, 章节与设定一键互转, 允许同作品内设定标题重复, 重名时仅拒绝而无自动消解, 新建设定正文强制空白或套用章节写作模板

**其它文件 (Other Files)**：
作品根目录下、与 `chapters/`、`settings/` 同级的自定义内容（例如作者自建的 `reference/` 参考资料目录，以及根目录其它松散文件）。侧栏以「其它文件」通用文件树管理这些同级目录与文件；支持在树内新建文件夹/文件（交互参考 VS Code 资源管理器），外部创建后经目录监视刷新。删除其它文件时进入**系统回收站**（对齐 VS Code 删文件常见行为），不进入作品垃圾箱。打开行为：常见图片类型在应用内预览；常规文本（如 md/txt/json 等）可编辑；PDF、Word（doc/docx）、Excel 提供**阅读/预览**（非完整办公编辑）；其余未知类型交系统默认程序或提示无法预览。可将适用的文本文件**移动到章节或设定**（写入对应目录并生成序号文件名，原位置移除）；**不提供**将章节/设定降格移回其它文件的一键能力（需删除后在其它文件中另行创建）。约定写作区目录（`chapters/`、`settings/`）与元数据区（`.teller`）不作为「其它文件」的主展示对象。
_Avoid_：只能在系统资源管理器中管理参考资料, 把 reference 等目录里的 .md 自动并入章节列表, 禁止用户在作品根下自建同级目录, 其它文件树只读不可新建, 其它文件删除进作品垃圾箱, 图片/PDF/Office 只能 Reveal 不能在应用内阅读, 把 Word/Excel 做成完整办公套件编辑器, 无法将参考文本升格为章节或设定, 提供章节/设定一键降格到其它文件

**快照 (Snapshot)**：
某部作品在某一时刻的可回看、可恢复的完整状态，升级为**类 git 的版本管理**：显式创建快照 ≈ git commit（可填备注）；引入 **branch（分支）**（平行剧情试写 + 大改安全网），支持 **checkout** 切换分支继续写作；**不提供自动 merge**（分支内容由作者手动取舍）；历史以**树状 DAG 图**可视化，支持快照间 / 快照与当前的文稿 diff。物理存放在该作品目录内的元数据区（如 `.teller/snapshots/`），采用内容寻址去重；快照范围含 `chapters/`、`settings/` 及根目录其它同级资料（如 `reference/`、`assets/`），实现须避免把快照库自身递归打进快照。自动保存不产生版本（仅防丢）。
_Avoid_：仅备份点无分支, 分支只读不可 checkout, 自动文本 merge, 无树状历史图, 版本库存应用全局不随作品走, 快照库递归自包含, 快照仅含章节设定而排除同级资料

**工作区 (Workspace)**：
一个 Teller 窗口所绑定的创作上下文，固定对应一部作品；其内可同时打开多个章节（及设定）进行编辑，并支持编辑器分组分屏（左右与上下，形成网格对照）。左侧资源侧栏以上下可折叠分区承载「章节」「设定」，并另有「其它文件」树管理根目录下自建同级目录（如 `reference/`）与其它松散文件。应用可同时存在多个窗口以并行编辑**不同**作品（一窗一部）；若再次打开已在某窗口打开的同一作品目录，则拒绝第二窗并聚焦已有窗口。再次打开某作品时，恢复先前的标签集合、激活标签、侧栏折叠、编辑器分屏布局与 AI 面板开闭等状态。
_Avoid_：多根工作区, 单窗挂载多部作品, 全局单例仅一部作品, 侧栏顶部分段一次只显示章节或设定, 同一作品目录多窗并行编辑, 每次打开作品都重置为空白布局, 仅多标签不可分屏, 侧栏完全无视根目录自建同级资料夹

**AI 面板 (AI Panel)**：
工作区内与编辑器并排的对话式 AI 区域，用于围绕当前作品提问、续写、改稿等，而不离开写作界面。默认上下文为「当前章节 + 该作品全部设定」；作者可按需追加引用其他章节，以及「其它文件」中可抽取文本的资料（交互仿照 Cursor 的 @/附加文件）；不默认把整个 `reference/` 等目录塞入每次对话。每部作品可有多条面板会话（新建、切换、归档/删除），再次打开该作品时可回到先前会话列表与内容。模型输出流式显示；可将回复「插入到光标处」或「应用到选区」（大块变更可预览确认）。**AI 与工作区之间的交互整体对齐 Cursor**（面板、内联、流式、接受/拒绝、插入/应用、附加上下文等），除非另有明文例外。未开启 Agent 模式时，面板改稿默认只落在当前文稿（或单一明确目标），不默认批量改写多文件。面板提供 **Ask / Agent** 手动切换（对齐 Cursor）。
_Avoid_：独立 AI 页, 外部聊天网页, 默认塞入全作品正文, 每部作品强制仅一条会话且不可并列, 每次打开作品 AI 会话强制空白且不可保留, 仅整段返回无流式, 面板结果只能手拷无法一键插入, 自创一套与 Cursor 冲突的 AI 编辑交互, 禁止引用其它文件资料, 默认全量注入 reference 目录, 面板默认无开关即批量改多章, 按章节强制绑定唯一会话


**内联 AI (Inline AI)**：
在编辑器正文中就地触发的 AI 能力（如选中后改写、续写），与 AI 面板互补；二者均为完整产品要具备的交互。改稿结果先以预览/对比呈现，作者接受或拒绝后再写入文稿（交互对齐 Cursor）；生成过程流式显示；接受后的修改进入正常撤销栈。失败时给出可读错误与重试/去设置等引导，且不阻断写作；请求可取消。普通内联默认只改当前文稿；跨文稿批量改写须切换到 Agent 模式。不提供边输入边出现的幽灵补全（Tab 灰字续写）；续写须由作者显式触发。
_Avoid_：仅侧栏对话, 仅命令面板, 无预览直接覆盖选区, AI 失败阻断写作, 静默失败无引导, 仅整段返回无流式, 内联默认静默改写多章, 打字时灰字自动续写


**Agent 模式 (Agent Mode)**：
AI 面板中与 **Ask 模式**并列、由作者**手动切换**的工作模式（对齐 Cursor 的 Ask / Agent 切换）。处于 Agent 时，允许模型在一轮中提议修改多个章节、设定及已附加的其它资料，并以变更集形式预览后全部或逐条接受/拒绝。处于 Ask 时，偏问答与单目标协助，不默认批量改写多文稿。界面须清晰显示当前模式。
_Avoid_：默认始终多文件改写无开关, 多文件改写无预览直接落盘, 与普通对话混为一体无法区分权限, 用独立会话类型替代 Ask/Agent 切换, 仅有 Agent 开关联名不叫 Ask


**写作规则 (Writing Rules)**：
归属某部作品、约束 AI 行为的持久说明（人称、文风、禁词、世界观红线等）；以作品目录元数据区内的规则文稿保存（如 `.teller/rules.md`），随作品备份与移动。AI 面板、内联 AI 与 Agent 模式默认带上该规则；作者可编辑，并可在单次对话中临时关闭应用规则。
_Avoid_：无作品级持久规则只能每次口述, 规则只存在应用全局不进作品目录, 规则不可临时关闭, 用设定条目冒充唯一规则机制却无法默认注入 AI

**写作审阅 (Writing Review)**：
内置的审阅技能（类比 Cursor 的 code review skill）：由作者在 AI 对话中手动触发；审阅结果作为**该会话中的一条（或多条）消息**返回（可疑点、依据等），**不**使用独立审阅/Problems 面板。作者随后通过同一对话决定：是否再生成一份审阅 Markdown 落盘、是否让 AI（含 Agent 模式）按审阅意见修改，或仅作参考不改文。
_Avoid_：独立审阅结果面板作为主呈现, 保存时自动全盘 lint 扫文, 审阅结果直接覆盖正文, 无审阅技能只能口头临时拼提示, 后台定时偷跑审阅, 强制每次审阅都落盘 md 或强制自动开改

**内置写作技能 (Built-in Writing Skills)**：
产品理想形态提供一组可手动触发的一等 AI 技能（结果均进入当前对话消息，交互范式与 Writing Review 相同），至少包括：写作审阅；续写本章；改写选区/段落；根据本章草稿更新设定（提议修改，预览后接受）；以及策划向技能（如开大纲/拆章建议、情节节拍检查等）。策划向输出是建议文本，**不**把大纲/细纲提升为与章节并列的一等结构。是否导出 Markdown、是否据此改稿，由作者在对话中决定。与作品技能一并经 AI 面板 **`/` 菜单**唤起。
_Avoid_：理想形态几乎无命名技能只靠自由聊天, 策划技能自动创建大纲一等公民, 技能结果默认绕过对话直接改多文件, 用独立工具窗替代对话式 skill 输出

**作品技能 (Work Skills)**：
归属某部作品的自定义 AI 技能，存放在作品目录元数据区（如 `.teller/skills/`），随作品备份与移动。作者可手写 skill 文稿，也可在对话中指挥 AI **生成或修改**当前作品的 skill；落盘后可与内置写作技能一样被点名触发，结果进入当前会话消息。唤起方式为 AI 面板输入 **`/`** 打开技能菜单（列出内置技能与本作品技能）。
_Avoid_：不可自定义只能用官方技能, 自定义技能只存在应用全局不进作品目录, 禁止通过对话生成作品技能, 生成 skill 时绕过作者确认直接覆盖且无预览, 仅靠自然语言猜测技能且无 / 菜单可发现入口



**文稿 (Manuscript)**：
章节或设定条目中可编辑的正文内容；以 Markdown 源码为权威形态与默认编辑视图，可切换预览或分屏预览；支持全量 GFM（含表格/任务列表/删除线等）+ 数学公式 + 图表（如 Mermaid）。
_Avoid_：富文本文档, Word 文档, 纯文本无结构, 默认所见即所得为唯一编辑方式

**作品目录 (Work Folder)**：
磁盘上代表一部作品的本地文件夹；打开某作品即打开该目录作为工作区根。根目录约定包含 `chapters/`（章节）与 `settings/`（设定）；作者可在根下自建同级目录（如 `reference/`）存放参考资料等。Teller 监视该目录的外部变更：无冲突时刷新，有未保存冲突时提示选择保留应用内编辑或加载磁盘版本。提供在系统资源管理器中显示当前文件或作品根目录的能力（对齐 VS Code Reveal）。打开尚无约定结构的文件夹时，询问是否初始化为作品；确认后写入约定目录再打开。
_Avoid_：应用私有库, 云端唯一真源, 数据库黑盒存储, 忽略外部改动, 冲突时静默强制重载, 无法从应用跳到磁盘位置, 打开任意文件夹时静默改目录且不询问, 对非作品文件夹只拒绝无初始化选项, 禁止根目录自建同级资料文件夹

**欢迎页 (Welcome)**：
未打开任何作品时的起始界面，提供新建作品、打开作品目录、最近打开列表，以及可选的 API Key 配置快捷入口（跳转到设置对应项，不强制配置即可写作）。新建作品时收集标题与保存位置，并生成约定**空**目录结构（不预置章节或设定文件）。最近打开默认保留约 10 部，支持固定与移除；路径失效时标记并允许重新定位或移除。可通过「作品 → 关闭作品」从工作区回到欢迎页（交互参考 VS Code 关闭文件夹）；依赖自动保存，仅保存失败时拦截关闭。打开作品不支持将文件夹拖入窗口。
_Avoid_：启动即强制打开文件夹对话框, 无首页直接空白工作区, 必须先填 Key 才能进入, 新建时强制填写模板/封面/笔名, 最近列表无限累加且不可管理, 关闭作品后留空白工作区不回欢迎页, 新建时自动插入「第一章」或设定模板文件, 拖入文件夹作为打开作品方式

**应用设置 (App Settings)**：
应用级偏好的统一配置页（主题、垃圾箱保留期、编辑器排版、AI API Key、快捷键等）。API Key 存于本机安全存储（如系统密钥链/加密存储），不写入作品目录明文文件。界面文案以简体中文为完整形态唯一支持语言。快捷键有默认方案，可在设置中查看与修改。应用更新走作者手动下载新版覆盖，不引入应用内自动更新机制。
_Avoid_：配置分散无统一入口, Key 明文写进作品文件夹, 完整形态必达中英双语, 快捷键写死不可自定义, 强制更新才能继续使用

**作品搜索 (Work Search)**：
在当前作品范围内按关键词检索并定位匹配处；为关键词全文搜索，不做语义搜索。默认搜索范围为章节与设定；可勾选将「其它文件」中可检索文本纳入搜索。查找/替换交互对齐 VS Code：当前文稿内可查找与替换；作品级批量替换默认仅作用于章节与设定（带结果预览与确认），避免误改参考资料。
_Avoid_：仅当前标签内查找, 无搜索, 语义检索, 无替换, 作品级替换无预览直接改写, 默认强制搜索全部其它文件且批量替换也改参考资料

**字数统计 (Word Count)**：
写作过程中的篇幅反馈：状态栏常显当前打开文稿的字数；可通过「作品 → 统计」或状态栏入口查看各章节字数与作品合计。计量口径可切换：仅汉字，或汉字 + 标点（字符数，对齐网文平台惯例），统计说明中写清当前口径。侧栏章节标题旁不常驻实时字数。
_Avoid_：无字数反馈, 侧栏每章标题旁实时刷字数

**崩溃恢复 (Crash Recovery)**：
异常退出后的防丢机制：以短间隔自动保存为主；若仍有未成功落盘的编辑缓冲，下次启动时提示恢复。
_Avoid_：无任何恢复提示, 完整多草稿崩溃会话恢复中心为必达

**本地优先隐私 (Local-First Privacy)**：
Teller 默认不采集使用遥测；作品正文与 API Key 不上传到 Teller 自有服务器。AI 请求仅发往作者配置的模型供应商。完整形态不要求账号或云同步。理想形态不引入 MCP 或通用外部工具协议，不依赖外挂工具生态。
_Avoid_：默认遥测, 正文上传 Teller 服务器, 强制账号云同步, 理想形态以 MCP/通用插件协议为必达扩展面


**主题 (Theme)**：
应用外观配色方案；默认为深色，可切换浅色，也可设置为跟随操作系统。
_Avoid_：仅深色不可切换, 强制唯一浅色

**导出 (Export)**：
将文稿输出为便于交稿或外传的文件：可导出当前打开的章节/设定为 Markdown 或纯文本；可将整部作品按章节顺序合并导出为一个 Markdown 或纯文本（设定可勾选是否并入；参考资料不塞进同一合并文件）；也可导出为文件夹副本（含 chapters、settings 与同级资料，默认排除快照库以免体积过大）。完整形态不包含 Word/PDF 排版导出或平台一键发布。
_Avoid_：无导出能力, Word/PDF 为首版必达, 平台一键发布, 合并导出硬塞全部参考资料, 文件夹副本默认打进全部快照历史

**编辑器排版 (Editor Typography)**：
文稿编辑区的可读性设置：可在应用设置中调整字体家族、字号、行高，以及可选的最大内容宽度；配色随主题。默认开启软换行（word wrap），可在设置中关闭。默认显示行号，可在设置中关闭。小地图（minimap）默认关闭，可在设置中开启。
_Avoid_：不可调排版, 稿纸模式, 竖排, 默认关闭软换行且强制横向滚动, 强制无行号且不可开, 默认开启小地图占宽

**专注模式 (Focus Mode)**：
写作时一键弱化工作台chrome：隐藏侧栏等非编辑区域，正文居中版心显示，便于沉浸写作；可退出回到完整三栏工作区。
_Avoid_：无沉浸写作视图, 强制始终无侧栏

**垃圾箱 (Trash)**：
作品目录内的暂存删除区；**仅**被删除的章节或设定进入此处。作者通过菜单「作品 → 垃圾箱」打开管理页（侧栏不设垃圾箱入口），可在列表中恢复、立即永久删除或清空；超过保留期后自动清除。默认保留约 30 天，可在应用设置中改为例如 7 / 30 / 90 天或永不自动清除。其它文件的删除不走此垃圾箱。
_Avoid_：仅用系统回收站处理章节/设定删除, 删除章节/设定即永久抹除, 应用全局统一垃圾箱, 侧栏常驻垃圾箱入口, 其它文件与章节共用作品垃圾箱
