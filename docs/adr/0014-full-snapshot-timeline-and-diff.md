# 快照完整形态：列表、时间线与文稿 diff

快照入口采用菜单「作品 → 保存快照 / 管理快照」（或等价侧栏入口）：创建时可填备注；列表中可选中恢复，恢复前确认将覆盖当前作品文件。

产品完整形态（PRD 范围，不限于某一迭代切片）还包含：

1. **时间线可视化** — 按时间浏览快照历史，而不是只有无结构的文件列表  
2. **对比 diff** — 快照与当前、或两快照之间，对章节/设定文稿做可读差异对比  

物理存放仍在作品目录元数据区；文稿自动保存与快照显式创建的边界不变。实现可分迭代交付，但需求上视为完整版本能力，而非永久砍掉。
