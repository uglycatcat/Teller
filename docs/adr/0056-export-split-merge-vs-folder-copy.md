# 导出：合并文件只含正文链路；文件夹副本含同级资料

完整产品导出分流：

- **当前文稿**：导出为 Markdown 或纯文本  
- **整书合并 MD/纯文本**：按章节顺序合并；设定可勾选是否并入；`reference/` 等参考资料不塞进同一合并文件  
- **文件夹副本**：包含 `chapters/`、`settings/` 与同级资料目录/文件；默认排除快照存储区（如 `.teller/snapshots/`），避免导出体积因历史快照膨胀  

不做 Word/PDF 排版导出或平台一键发布。
