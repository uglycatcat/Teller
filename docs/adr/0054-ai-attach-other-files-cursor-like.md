# 其它文件进入 AI 上下文的方式对齐 Cursor

作者可将「其它文件」（如 `reference/` 下可抽取文本的 PDF、Word、Excel、普通文本等）显式附加到 AI 面板上下文，交互对齐 Cursor 的 @/附加文件模式。默认上下文仍是「当前章节 + 全部设定」，不自动全量注入参考目录。

仅将能够抽取文本的内容送入模型；图片等默认不作为正文上下文（除非后续按 Cursor 能力扩展且另有说明）。无法抽取文本时给予明确提示。

此条是 ADR 0041（AI 与工作区交互对齐 Cursor）在参考资料场景下的具体化。
