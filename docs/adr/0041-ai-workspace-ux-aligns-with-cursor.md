# AI 与工作区交互对齐 Cursor

完整产品中，AI 与写作工作区的交互（含 AI 面板、内联 AI、流式输出、预览接受/拒绝、插入光标、应用到选区、请求取消、失败引导等）以 Cursor 为对齐基准，不另造一套冲突心智。

面板生成结果支持「插入到光标处」与「应用到选区」；大块变更可预览确认。上下文范围等 Teller 领域规则（如默认「当前章节 + 全部设定」）仍按既有 ADR，不因对齐 Cursor 而改为默认塞入全作品正文。

除非后续 ADR 明文写出例外，否则此类交互细节默认按 Cursor 行为解释，grill 中不必逐条重复确认。
