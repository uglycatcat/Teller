# 文稿以自动保存为主；快照需显式创建

写作过程中章节与设定文稿自动落盘（短间隔，并在失焦或切换标签时保存），降低崩溃与遗忘导致的丢失。快照不随每次自动保存产生，而由作者显式创建，避免快照爆炸并保持「可恢复点」有意义。

> **修订（2026-08-25）**：显式快照 ≈ git commit；自动保存仍不产生版本（边界不变）。详见 `docs/prd/08-version-control.md`。
